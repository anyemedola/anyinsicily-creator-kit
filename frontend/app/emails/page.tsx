import EmailsContent from '@/components/emails/EmailsContent';

export const metadata = {
  title: 'Pitch Emails — Any in Sicily Creator Kit',
};

export default function EmailsPage() {
  return <EmailsContent />;
}
