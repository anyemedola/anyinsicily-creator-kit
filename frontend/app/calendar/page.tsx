import CalendarContent from '@/components/calendar/CalendarContent';

export const metadata = {
  title: 'Calendário — Any in Sicily Creator Kit',
};

export default function CalendarPage() {
  return <CalendarContent />;
}
